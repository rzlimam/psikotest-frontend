import { User } from './User';
import { QuestionType } from './QuestionType';

export class PointForEachType {
    pointTypeId: string;
    user: User;
    questionType: QuestionType;
    totapPointForEachType: number;
}